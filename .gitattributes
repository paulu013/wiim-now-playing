# Keep LF line endings on files that run on the Raspberry Pi (Linux). A CRLF
# shebang would break bash/python3 there, so these must never be checked out CRLF.
*.sh            text eol=lf
*.py            text eol=lf
*.service       text eol=lf
deploy/pi/*     text eol=lf
